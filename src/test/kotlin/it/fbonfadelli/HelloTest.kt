package it.fbonfadelli

import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

class HelloTest {
    @Test
    internal fun `it works`() {
        assertThat(true).isEqualTo(true)
    }
}
